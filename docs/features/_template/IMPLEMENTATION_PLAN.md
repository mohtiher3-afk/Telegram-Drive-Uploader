# Implementation Plan

Implementation begins only after the feature request, discovery, impact analysis, requirements, user flow, and technical design are reviewed.

| Step | Files | Change | Risk | Verification |
|---:|---|---|---|---|
| 1 | `<paths>` | `<smallest safe change>` | LOW / MEDIUM / HIGH / CRITICAL | `<focused test>` |
| 2 | `<paths>` | `<smallest safe change>` | LOW / MEDIUM / HIGH / CRITICAL | `<focused test>` |
| 3 | `<paths>` | `<smallest safe change>` | LOW / MEDIUM / HIGH / CRITICAL | `<focused test>` |

After each logical group, compile, run focused tests, inspect the diff, and stop on a critical failure. Before release, run lint, security, TDLib/resource checks as applicable, the master self-check, regression validation, and documentation review.
