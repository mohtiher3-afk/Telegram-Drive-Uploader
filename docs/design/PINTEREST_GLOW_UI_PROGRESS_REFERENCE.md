# Pinterest Glow UI Progress Reference

## Evidence boundary

The supplied Pinterest search page was opened. Its rendered content lists Glow UI-related interest terms including **Neon**, **Animation**, **Blue**, **Ambient**, **Dark**, and **Soft Inner UX**, and exposes a grid of thumbnail pins. The accessible browser output did not provide a reliably inspectable full-size pin or exact visual tokens. The direction below is therefore tentative and limited to the verified search framing plus the user’s stated intent.

## Safe direction for real upload progress

| Search evidence / request | Telegram Drive translation | Guardrail |
|---|---|---|
| Blue, ambient, dark glow framing | Low-opacity cobalt halo around the current fill endpoint. | Halo does not alter fill length, percentage, or semantic range. |
| Animation | Slow opacity/radius pulse only while actual `UPLOADING` state is active. | No motion when Android animator scale is off. |
| Soft inner visual treatment | Existing track stays visible behind a small endpoint bead. | Track and textual percentage remain sufficient without color. |

## Non-transfers

No search thumbnail or keyword is treated as authorization to add neon-dominated decoration, fake loading, artificial percentages, agent behavior, unrelated controls, or nonfunctional effects. The upload progress source remains persisted real upload state.
